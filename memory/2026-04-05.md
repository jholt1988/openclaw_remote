# 2026-04-05

## Durable session outcomes

- Implemented and shipped major backend hardening work in `pms-master` (tenant_portal_backend), pushing directly to `main` across many incremental commits.

### Payments / accounting boundary delivered
- Added operational ledger foundation:
  - `LedgerAccount` + `LedgerTransaction` models and migrations.
  - Posting hooks for invoice charges, payments, manual charges/payments, and reversal entries.
- Added APIs:
  - `GET /payments/ledger/accounts/:leaseId`
  - `GET /payments/delinquency/queue` with pagination (`limit/offset`) and sorting (`daysPastDue`, `amountDueCents`, `tenantName`, `priorityScore`).
- Added delinquency `priorityScore` plus configurable weighting.
  - Env defaults: `DELINQUENCY_PRIORITY_DAYS_WEIGHT`, `DELINQUENCY_PRIORITY_AMOUNT_WEIGHT`.
  - Org-level override fields on `Organization`: `delinquencyDaysWeight`, `delinquencyAmountWeight`.
  - New admin endpoints:
    - `GET /payments/delinquency/priority-config`
    - `POST /payments/delinquency/priority-config`
- Added idempotency and integrity:
  - Unique txn key for ledger posting.
  - Write-time reversal guardrails (valid original, opposite direction, one reversal per original, positive amounts).
- Added reconciliation tooling:
  - `npm run ledger:backfill` (idempotent historical backfill)
  - `npm run ledger:reconcile` (dry run)
  - `npm run ledger:reconcile:apply` (repair legacy lease balances from ledger truth)
- Ran migration/backfill/reconcile in environment; fixed a real mismatch where lease balance was 0 but ledger truth was 810000 cents.

### Leasing / applications delivered
- Rental application intake hardening:
  - Required legal accepts + verification authorizations.
  - Required uploads and minimum income/employment completeness checks.
  - Persisted compliance evidence fields on application record.
- Screening/review hardening:
  - Added structured denial reason codes and required denial reason enforcement.
  - Persisted decision metadata (`decisionReasonCode`, `decisionNotes`, `decisionedAt`).
- Lead application pipeline hardening:
  - Valid status transition guardrails.
  - Decision statuses require reviewer.
  - Added stale follow-up fields (`lastActivityAt`, `followUpDueAt`) and stale queue endpoint:
    - `GET /applications/stale/follow-ups`
- Added approved application → lease conversion:
  - New endpoint: `POST /rental-applications/:id/convert-to-lease`
  - Creates draft lease, links application via `convertedLeaseId`, writes lease history + audit log.

### E-signature hardening delivered
- Added strict pre-send lease merge-field validation before envelope creation.
- Added envelope status lifecycle timeline tracking in metadata (status timeline + sent/delivered/completed/etc timestamps).
- Improved webhook participant status sync using webhook participant payload when present.
- Auto-promotes lease to `ACTIVE` on completed envelope (idempotent), writing lease history and metadata flag (`leasePromotedAt`).
- Reminder engine upgraded to milestone cadence + backoff:
  - Milestones (default `24h,4h` before due), max reminders, exponential backoff on failures.
- Added ops visibility endpoint:
  - `GET /esignature/risk-queue` for near-due signature risk monitoring.

## Cron jobs added
- Daily ledger reconciliation (dry-run):
  - Job ID: `12175768-730a-4593-aa28-f26aaa59cacf`
- Daily stale lead-application follow-up scan:
  - Job ID: `a0bf21fa-769c-4483-90c5-af772794b505`
- Daily e-signature risk summary:
  - Job ID: `28e5e99d-c579-4d9d-906c-2616efa12327`

## Notable workflow preference established
- User repeatedly requested rapid incremental execution with "proceed"; accepted direct implementation, validation, commit, and push cycles without lengthy planning overhead.

- User clarified reporting focus: day-to-day operations reports for pms-master/property-pulse (de-prioritize manager/admin strategic framing for now).

- Set recurring daily reminder/report trigger at 8:00 PM America/Chicago for Property-Pulse day-to-day ops report (chat + workspace file outputs).
- User requested to revisit earlier user stories tasks next.

- Built memory-only recovery backlog for user stories with focus on A-series manual UX closure (A-03, A-06, A-07, A-09, A-10, A-11). File: reports/user-stories-recovery-A-series.md

- A-03 evidence capture started: screenshots + screen recording created under reports/evidence/A-03. Login currently remains on /login after submit despite valid API auth at :3001; requires frontend auth-flow debugging before A-03 can be marked PASS.
- Identity/profile finalized: assistant name set to Aiden with Rigorous Strategic Mentor style; user confirmed as Jordan (America/Chicago).
- Workspace bootstrap completed: BOOTSTRAP.md deleted; git repository initialized in /data/.openclaw/workspace; branch renamed to main; git identity configured locally (jholt1988 / jordanh316@gmail.com).
- Reporting preference locked: daily Property-Pulse day-to-day ops report at 8:00 PM America/Chicago with dual output (chat + workspace file). Cron job created: "Daily Property-Pulse Ops Report (8:00 PM US Central)".
- Scope correction captured: user shifted from manager/admin reporting to day-to-day operations reporting for pms-master/property-pulse.
- User-story recovery lane activated from memory history only; A-series closure queue established for A-03, A-06, A-07, A-09, A-10, A-11.
- Seed-script stabilization completed in pms-master/tenant_portal_backend:
  - Rewrote prisma/seed-demo.ts to align with current schema.
  - Fixed lease cleanup dependency ordering (ledger accounts before lease delete).
  - Improved demo seed verification robustness across multiple Unit 101 rows.
- A-03 status: PASS.
  - Root cause fixed: frontend dev auth flow was blocked by auto-enabled MSW; changed behavior to explicit opt-in (VITE_USE_MSW === 'true').
  - Evidence captured (recording + screenshots) under reports/evidence/A-03.
- A-06 status: PASS.
  - Implemented Option A: lease-linked tenant documents surfaced directly in My Lease (without opening tenant /documents route).
  - Evidence captured under reports/evidence/A-06.
- A-07 status: PASS.
  - Full in-UI tenant maintenance create + photo upload flow verified; PM queue/detail photo visibility verified.
  - Evidence captured under reports/evidence/A-07.
- A-09 status: PARTIAL.
  - Attachment support added to active shared messaging page (composer input + attachment rendering).
  - Tenant-side send-with-attachment and audit metadata logging verified.
  - Remaining blocker: PM inbox did not surface new thread deterministically in automation run; PM-side UI attachment visibility still needs closure.
- Latest direction from user before flush: continue strict-order execution and proceed, with preference to close current story blockers to true PASS when possible.

- Closed A-09 to PASS for property-pulse: fixed backend messaging overfilter in participant checks/listing, verified tenant->PM thread visibility with attachment rendering on both sides, and confirmed message metadata attachments persisted for auditability.

- Closed A-10 to PASS: verified owner minimum constraints (propertyId required on create, notes allowed, status/assignment blocked for owner org role), with UI list/detail evidence and recording under reports/evidence/A-10.

- Closed A-11 to PASS: verified mobile-first tenant inspection draft persistence/restoration (localStorage key tenant-inspection-draft:<inspectionId>, restore notice banner, dismiss flow) with iPhone viewport evidence under reports/evidence/A-11.

- Started tenant-first property-pulse frontend integration pass. Implemented messaging attachment parity in imported/property-pulse tenant inbox (compose attachment URLs + render attachment links from metadata). Commit: 212829ed.
